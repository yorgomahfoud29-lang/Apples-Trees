Apples & Trees — local dev

How to preview:

1. Open any `*.html` file (for example `index.html`) in your browser (double-click or drag into browser).
2. Files added:
   - `favicon.svg` — site icon used in browsers
   - `og-image.svg` — default Open Graph image for sharing
   - `script.js` — simple JS to toggle the mobile menu
      - `favicon.svg` — simple SVG favicon
      - `og-image.svg` — default Open Graph image for sharing
      - `script.js` — simple JS to toggle the mobile menu and initialise AOS animations
      - AOS (Animate On Scroll) and Font Awesome are included via CDN for animations and icons

What changed:
- Responsive header with hamburger menu for small screens
- Skip-to-content link for accessibility
- Meta descriptions and OG tags for better sharing
- Card-based layout and improved typography
 - AOS animations for subtle scroll animations
 - Font Awesome for iconography (CDN)

If you want, I can also:
- generate a PNG favicon (ICO) for maximum browser support
- create a French-only localized version
- deploy to GitHub Pages and prepare a CNAME
